name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install platformio
        run: pip install platformio

      - name: Run the unit tests on the host
        run: pio test -e native

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache the platformio toolchain
        uses: actions/cache@v4
        with:
          path: |
            ~/.platformio
            .pio
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}

      - name: Install platformio
        run: pip install platformio

      - name: Provide the configuration header
        run: |
          if [ -f include/config.h.example ] && [ ! -f include/config.h ]; then
            cp include/config.h.example include/config.h
          fi

      - name: Build the firmware
        run: pio run -e esp32dev

      - name: Static analysis
        run: pio check -e esp32dev --skip-packages --fail-on-defect high
